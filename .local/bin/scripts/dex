#!/bin/sh

# ssh exit from dwm for black display after suspend

# For non-systemd init systems.

kill -TERM $(pidof dwm)

# kill -TERM $(pgrep -u $USER "\bdwm$")
# kill -HUP $(pgrep -u $USER "\bdwm$")

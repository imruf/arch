#!/usr/bin/env bash


url="$*"

echo "Fetching Code..."
youtube-dl -F "$url"
read -p "Enter Code: " FORMAT

echo "Streaming..."
mpv --ytdl-format=$FORMAT "$url" --stream-record=1.ts
#--stream-record=1.ts
#--start=00:00:00 

#!/bin/bash
# switch power state using dmenu(1)

selection=$(printf 'lock\nsuspend\nshutdown\nreboot' \
		| dmenu  -i -p ":>_" \
)

case "${selection}" in
	lock|shutdown|reboot|suspend)
		dwmexit ${selection}
		;;
esac

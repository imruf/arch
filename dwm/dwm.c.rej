--- dwm.c	2008-11-17 18:09:29.000000000 +0100
+++ dwm.c	2008-09-09 21:46:17.000000000 +0200
@@ -517,8 +515,13 @@ drawbar(void) {
 	}
 	else
 		x = dc.x;
-	stextw = drawstatustext(x);
-	dc.x = ww - stextw;
+	dc.w = TEXTW(stext);
+	dc.x = ww - dc.w;
+	if(dc.x < x) {
+		dc.x = x;
+		dc.w = ww - x;
+	}
+	drawtext(stext, dc.norm, False);
 	if((dc.w = dc.x - x) > bh) {
 		dc.x = x;
 		if(sel) {
